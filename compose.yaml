services:
  bookmarker:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: bookmarker
    ports:
      - "3000:3000"
    volumes:
      - bookmarker-data:/data
    restart: unless-stopped

volumes:
  bookmarker-data:
